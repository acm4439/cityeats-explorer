# Local dev: Vite proxy reads this and attaches Authorization (see vite.config.ts)
VITE_YELP_API_KEY=your_api_key_here

# Vercel (production): set YELP_API_KEY in Project → Settings → Environment Variables.
# Do not use the VITE_ prefix for the server secret. Same Yelp Fusion key as local.
